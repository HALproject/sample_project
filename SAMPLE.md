```
demo-app/                     <- デモ専用 React プロジェクト
├─ public/
│   ├─ index.html             <- React のルート HTML
│   ├─ オペレーションルーム.png  <- 背景画像
│   └─ assets/
│       └─ ope.svg            <- オペレーター用ピクトグラム
│
├─ src/
│   ├─ main.jsx               <- React エントリーポイント
│   ├─ App.jsx                <- ルーティング管理
│   └─ DemoScreen.jsx         <- /demo で表示するデモ画面
│
├─ package.json
└─ tailwind.config.js         <- Tailwind CSS 設定（必要なら）

```